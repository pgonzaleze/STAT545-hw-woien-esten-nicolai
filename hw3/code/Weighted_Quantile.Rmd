---
title: "Code documentation"
output: html_document
---

### Abstract
This document includes documentation of the functions constructed for the purpose.

### Table of Content



## Weighted Quantile


```{r, eval=FALSE}
weighted.quantile <- function(x, probs=seq(0, 1 ,0.25), weight=rep(1,length(x))) {
  
  x <- sort(x)
  n.probs    <- length(probs)
  quantiles  <- numeric(n.probs)
  cum.weight <- cumsum(weight)
  cum.weight <- cum.weight/tail(cum.weight,n=1)
  
  for (i in 1:n.probs) {
    p = probs[i]
    
    for (j in 1:length(cum.weight)) {
      if (cum.weight[j] >= p) {
        quantiles[i] = x[j]
        break
      }
    }
  }
  return(quantiles)
}

```

The weighted quantile is computed with the inverse cumulative distribution method. The weights are normalized, and then considered 

